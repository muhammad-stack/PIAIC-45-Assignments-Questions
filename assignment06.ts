//  Following will be the name used by me

const persons_Name : string = "\tMuhammad Ali\n"

// Following line of code will do the first part addingg whitespaces to the beginning and at the end 

console.log(` The person's name with whitespaces is displayed as : ${persons_Name}`);

//  Following line of code will remove the whitespaces from the beginning and ath the end and then name will be displayed as :-

console.log(` The person's name after stripping the whitespaces will be:-${persons_Name.trim()}`);

