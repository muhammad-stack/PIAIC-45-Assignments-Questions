let names = ["umer","shaheer","asad","umair"];
for(var i=0;i<8;i++){
    if(names[i] === "umer"){
        console.log(`hello what's up ${names[0]}`);   
    };
    if(names[i] === "shaheer"){
        console.log(`hello what's up ${names[1]}`);
    }
    if(names[i]=== "asad"){
        console.log(`hello what's up ${names[2]}`);
        
    }
    if(names[i] === "umair"){
        console.log(`hello what's up ${names[3]}`);
        
    }

}